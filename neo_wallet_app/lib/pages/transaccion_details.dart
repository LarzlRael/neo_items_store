import 'package:flutter/material.dart';

class TransaccionDetails extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('TransaccionDetails'),
      ),
    );
  }
}
